export default ({ app }) => {
    // Get localized path for homepage
    // Get path to switch current route to French
    const switchLocalePath = (lang_code)=>{app.i18n.switchLocalePath(lang_code)}
    
}